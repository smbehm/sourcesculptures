/**
 * Site-wide build counter for the deploy badge. Bump this by **1** before each
 * production deploy so desktop and mobile always show the same consecutive
 * number for that release (no git SHA / env mismatch between devices).
 */
export const SITE_BUILD_NUMBER = 1;
